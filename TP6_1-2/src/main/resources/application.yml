server:
  port: 8082

spring:
  datasource:
    url: jdbc:h2:mem:banque
    username: user
    password: user
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
